@model IEnumerable<DiplomadoFinal.Models.Curso>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Grado)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Anoescolar.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Seccion.Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Grado)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Anoescolar.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Seccion.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CursoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CursoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CursoID })
        </td>
    </tr>
}

</table>
