@model IEnumerable<DiplomadoFinal.Models.IncripcionCurso>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Curso.Grado)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Usuario.Nombres)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Curso.Grado)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Usuario.Nombres)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IncripcionCursoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.IncripcionCursoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IncripcionCursoID })
        </td>
    </tr>
}

</table>
